# Инструкция по выполнению домашних заданий

Привет! Это твой репозиторий с домашними заданиями по курсу AQA Python.
Здесь лежат папки с уроками, в каждой из которых есть файл `homework.py` с задачами.

## Шаг 1. Подготовка (делается один раз)

Чтобы вы могли сохранять свои решения и отправлять их на проверку, вам нужно сделать свою копию этого репозитория (**Fork**).

1.  **Сделайте Fork (Копию)**:
    *   В правом верхнем углу этой страницы найдите кнопку **Fork** (Вилка).
    *   Нажмите на неё.
    *   В открывшемся окне нажмите **Create fork** (Создать копию).
    *   Теперь у вас в профиле появится ТОЧНО ТАКОЙ ЖЕ репозиторий, но это будет ВАША личная копия. 

2.  **Склонируйте ВАШ репозиторий к себе на компьютер**:
    *   Находясь на странице **ВАШЕГО** нового репозитория (убедитесь, что в адресе браузера ваше имя пользователя), нажмите зеленую кнопку **<> Code**.
    *   Скопируйте ссылку (она выглядит как `https://github.com/ВАШ_НИК/AQA_py.git`).
    *   Откройте терминал (или Git Bash) на компьютере и напишите команду:
        ```bash
        git clone https://github.com/ВАШ_НИК/AQA_py.git
        ```
    *   Зайдите в скачанную папку:
        ```bash
        cd AQA_py
        ```
    *(Если вы используете PyCharm/VS Code, можно просто нажать "Get from VCS" или "Clone Repository" и вставить вашу ссылку там)*.

---

## Шаг 2. Выполнение домашнего задания

Когда вы посмотрели урок и готовы делать практику:

1.  **Откройте проект** в PyCharm или VS Code.
2.  Найдите папку нужного урока (например, `Lesson_01_Basics`).
3.  Откройте файл `homework.py`.
4.  Читайте задачи в комментариях и пишите код прямо под ними.

Пример:
```python
# Задание: Сложите 2 и 2
# Ваш код:
result = 2 + 2
print(result) 
```

---

## Шаг 3. Как сдать работу

Когда вы решили задачи:

1.  **Сохраните изменения (Commit)**:
    Откройте терминал в папке проекта и выполните команды по очереди:
    
    *   Добавляем все измененные файлы:
        ```bash
        git add .
        ```
    
    *   Сохраняем их с комментарием (в кавычках напишите, что сделали):
        ```bash
        git commit -m "Сделал домашку по уроку 1"
        ```

2.  **Отправьте на сервер (Push)**:
    ```bash
    git push origin main
    ```
    *После этой команды ваше решение появится в вашем репозитории на GitHub.*

3.  **Отправьте на проверку (Pull Request)**:
    *   Зайдите в ВАШ репозиторий на GitHub.
    *   Вы увидите желтую плашку **Compare & pull request** (она появляется недавно после push). Нажмите её.
    *   Или перейдите во вкладку **Pull requests** -> **New pull request**.
    *   Убедитесь, что стрелочка показывает: `base: MikeDragonCode/AQA_py` <- `compare: ВАШ_НИК/AQA_py`.
    *   Нажмите **Create pull request**.
    *   Ссылку на созданный PR (Pull Request) отправляйте преподавателю/ментору.

Удачи! 🚀

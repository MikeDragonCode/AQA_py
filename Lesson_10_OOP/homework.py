# ПАМЯТКА: ООП
# class Page:
#     def __init__(self, driver):
#         self.driver = driver
#
#     def open(self):
#         print("Opening Page")
#
# Page Object Model (POM): Разделение логики страниц и тестов.

# Домашнее задание: ООП (AQA Focus - Page Object Model Intro)

# 1. Класс Элемент:
# Создайте класс `Button`.
# В конструкторе (__init__) задайте атрибуты `text` и `locator`.
# Создайте объект кнопки "Login" с локатором "//button[@id='login']".
# Ваш код:

# 2. Методы действий:
# Добавьте в класс `Button` метод `click()`, который выводит "Clicking button {text}".
# Вызовите этот метод у своей кнопки.
# Ваш код:

# 3. Базовый класс (BasePage):
# Создайте класс `BasePage`. В конструкторе сохраните URL страницы.
# Добавьте метод `visit()`, который выводит "Visiting {url}".
# Ваш код:

# 4. Наследование в POM:
# Создайте класс `LoginPage`, который наследуется от `BasePage`.
# В LoginPage добавьте метод `login(user, password)`, который выводит "Logging in with {user}".
# Создайте объект LoginPage и вызовите сначала visit(), а потом login().
# Ваш код:

# 5. Скрытые детали (Инкапсуляция):
# В классе `LoginPage` создайте приватный атрибут `__api_key`.
# Напишите метод для его получения `get_api_key()`.
# Попробуйте изменить его напрямую снаружи класса.
# Ваш код:

# 6. Список объектов:
# Создайте список `pages`, содержащий объекты LoginPage, ProfilePage и HomePage.
# Пройдитесь циклом и у каждой страницы вызовите метод visit() (используя полиморфизм).
# Ваш код:

# 7. Отчет об объекте (__str__):
# Переопределите метод `__str__` в классе `BasePage`, чтобы при печати объекта выводилось: "Page object for URL: {url}".
# Ваш код:

# 8. Атрибуты класса (Config):
# Создайте класс `APIConfig`. Добавьте атрибут класса `BASE_URL = "https://api.test.com"`.
# Добавьте метод класса, который возвращает полный путь к энпоинту /users.
# Ваш код:

# 9. Фабрика драйверов (простой пример):
# Создайте класс `Driver`. В конструкторе принимайте `browser_type`.
# Если браузер не передан - по умолчанию 'chrome'.
# Ваш код:

# 10. Декораторы (Бонус):
# Используйте @property для создания метода `is_loaded` в классе `BasePage`, 
# который будет возвращать True (симуляция проверки загрузки страницы).
# Ваш код:

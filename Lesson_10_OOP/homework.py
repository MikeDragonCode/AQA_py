# ПАМЯТКА: ООП
# class Cat:
#     def __init__(self, name):
#         self.name = name  # атрибут экземпляра
#
#     def meow(self):       # метод
#         return "Meow!"
#
# my_cat = Cat("Barsik")  # создание объекта (экземпляра)
# print(my_cat.name)      # доступ к атрибуту
#
# Наследование: class Lion(Cat): ...
# Инкапсуляция: self.__secret (приватный атрибут)

# Домашнее задание: ООП (Объектно-ориентированное программирование) (Основы)

# 1. Создание класса:
# Создайте класс `Dog`.
# Напишите метод `__init__`, который принимает `name` и `age`.
# Создайте экземпляр класса `Dog` с именем "Бобик" и возрастом 3.
# Ваш код:

# 2. Методы класса:
# В классе `Dog` добавьте метод `bark`, который возвращает строку "Гав!".
# Вызовите этот метод у экземпляра.
# Ваш код:

# 3. Атрибуты экземпляра:
# Создайте класс `Person`.
# В конструкторе установите атрибут `name`.
# Добавьте метод `greet`, который выводит "Привет, меня зовут <name>".
# Ваш код:

# 4. Наследование:
# Создайте класс `Animal` с методом `speak`, который возвращает пустую строку.
# Создайте класс `Cat`, который наследуется от `Animal`.
# Переопределите метод `speak` в классе `Cat`, чтобы он возвращал "Мяу".
# Ваш код:

# 5. Инкапсуляция (Private атрибуты):
# Создайте класс `BankAccount`.
# В конструкторе инициализируйте ПРИВАТНЫЙ атрибут `__balance` (с двумя подчеркиваниями).
# Напишите методы `deposit(amount)` для пополнения и `get_balance()` для получения баланса.
# Попробуйте изменить баланс напрямую (bank_account.__balance = 1000) и проверьте, изменился ли он через get_balance().
# Ваш код:

# 6. Полиморфизм (простой пример):
# Создайте список животных `animals` (экземпляры Cat и Dog из предыдущих заданий).
# Пройдитесь циклом по списку и вызовите метод `speak()` (или bark/meow, если унифицировали интерфейс) для каждого.
# Ваш код:

# 7. Метод __str__:
# В классе `Person` (или новом классе Book) переопределите метод `__str__`,
# чтобы при `print(object)` выводилось красивое описание объекта, а не "<__main__.Person object at ...>".
# Ваш код:

# 8. Атрибуты класса (статические):
# Создайте класс `Circle`.
# Добавьте атрибут класса `pi = 3.14`.
# Создайте метод `area(radius)`, который использует `Circle.pi`.
# Ваш код:

# 9. Конструктор с параметрами по умолчанию:
# Создайте класс `Rectangle`.
# Конструктор должен принимать width и height, но если height не передан, он должен быть равен width (квадрат).
# Ваш код:

# 10. Свойства (Properties) (Бонус):
# Используйте декоратор @property для создания геттера `fullname` в классе `User`,
# который объединяет `first_name` и `last_name`.
# Ваш код:
